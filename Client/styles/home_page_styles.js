import { StyleSheet } from "react-native";

const home_styles = StyleSheet.create({
  container: {
    alignItems: "center",
    height: "100%",
    display: "flex"
  },
});

export default home_styles;
