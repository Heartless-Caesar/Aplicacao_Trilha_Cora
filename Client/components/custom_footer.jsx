import { View, SafeAreaView } from "react-native";
import CustomFooterStyle from "../styles/custom_footer_style";

const CustomFooter = () => {
    return (
        <View style={CustomFooterStyle.footer} />
    );
};

export default CustomFooter;
